# calculator IOS from React-Native


> App exercise in React Native, copy of Calculator IOS, run in IOS Emulator.

Commands:

```
git clone git@github.com:rodycastillo/calculator-RN.git
yarn install
npx react-native run-ios
```
